/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsListResourcesByContext } from "../../funcs/assetsListResourcesByContext.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListResourcesByContextRequest$inboundSchema,
};

export const tool$assetsListResourcesByContext: ToolDefinition<typeof args> = {
  name: "assets-list-resources-by-context",
  description: `Get resources by context

Retrieves resources matching specific context key/value pairs.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsListResourcesByContext(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
