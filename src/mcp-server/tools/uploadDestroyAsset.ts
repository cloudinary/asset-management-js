/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadDestroyAsset } from "../../funcs/uploadDestroyAsset.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DestroyAssetRequest$inboundSchema,
};

export const tool$uploadDestroyAsset: ToolDefinition<typeof args> = {
  name: "upload-destroy-asset",
  description: `Destroys an asset/resource`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await uploadDestroyAsset(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
