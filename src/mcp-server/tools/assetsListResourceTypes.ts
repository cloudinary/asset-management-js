/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsListResourceTypes } from "../../funcs/assetsListResourceTypes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListResourceTypesRequest$inboundSchema,
};

export const tool$assetsListResourceTypes: ToolDefinition<typeof args> = {
  name: "assets-list-resource-types",
  description: `Get resource types

Returns a list of all resource types that correspond to assets currently in your product environment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsListResourceTypes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
