/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { tasksGetGenerationTaskStatus } from "../../funcs/tasksGetGenerationTaskStatus.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  taskId: z.string(),
};

export const tool$tasksGetGenerationTaskStatus: ToolDefinition<typeof args> = {
  name: "get-generation-task",
  description: `Get a generation task

Get the status of a generation task.`,
  scopes: ["builder"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksGetGenerationTaskStatus(
      client,
      args.taskId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
