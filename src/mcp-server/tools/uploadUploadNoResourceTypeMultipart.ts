/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadUploadNoResourceTypeMultipart } from "../../funcs/uploadUploadNoResourceTypeMultipart.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.BinaryUploadRequest$inboundSchema,
};

export const tool$uploadUploadNoResourceTypeMultipart: ToolDefinition<
  typeof args
> = {
  name: "upload-upload-no-resource-type-multipart",
  description: `Upload with automatic file type detection

Uploads a file to Cloudinary. The file type is automatically detected based on its content, so you don't need to specify the type manually.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await uploadUploadNoResourceTypeMultipart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
