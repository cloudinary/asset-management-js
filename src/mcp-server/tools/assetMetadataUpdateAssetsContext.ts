/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetMetadataUpdateAssetsContext } from "../../funcs/assetMetadataUpdateAssetsContext.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  resourceType: components.ResourceType$inboundSchema,
  contextUpdateRequest: components.ContextUpdateRequest$inboundSchema,
};

export const tool$assetMetadataUpdateAssetsContext: ToolDefinition<
  typeof args
> = {
  name: "manage-asset-context",
  description: `Adds or clears contextual metadata on multiple assets

Applies a contextual-metadata command to the given assets, addressing them by public ID.
`,
  scopes: ["librarian"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetMetadataUpdateAssetsContext(
      client,
      args.resourceType,
      args.contextUpdateRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
