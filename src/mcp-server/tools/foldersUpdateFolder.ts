/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { foldersUpdateFolder } from "../../funcs/foldersUpdateFolder.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  folder: z.string(),
  moveFolderRequest: components.MoveFolderRequest$inboundSchema,
};

export const tool$foldersUpdateFolder: ToolDefinition<typeof args> = {
  name: "move-folder",
  description:
    `Renames or moves an entire folder (along with all assets it contains) to a new location

Renames or moves an entire folder (along with all assets it contains) to a new location within your Cloudinary media library.`,
  scopes: ["librarian"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await foldersUpdateFolder(
      client,
      args.folder,
      args.moveFolderRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
