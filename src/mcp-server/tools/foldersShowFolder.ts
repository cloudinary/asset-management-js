/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { foldersShowFolder } from "../../funcs/foldersShowFolder.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ShowFolderRequest$inboundSchema,
};

export const tool$foldersShowFolder: ToolDefinition<typeof args> = {
  name: "folders-show-folder",
  description: `List sub-folders

Returns a list of sub-folders under the specified folder path.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await foldersShowFolder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
