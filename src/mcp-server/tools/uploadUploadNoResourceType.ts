/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadUploadNoResourceType } from "../../funcs/uploadUploadNoResourceType.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UploadRequest$inboundSchema,
};

export const tool$uploadUploadNoResourceType: ToolDefinition<typeof args> = {
  name: "upload-upload-no-resource-type",
  description: `Upload with automatic file type detection

Uploads a file to Cloudinary. The file type is automatically detected based on its content, so you don't need to specify the type manually.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await uploadUploadNoResourceType(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
