/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadUploadMultipart } from "../../funcs/uploadUploadMultipart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UploadMultipartRequest$inboundSchema,
};

export const tool$uploadUploadMultipart: ToolDefinition<typeof args> = {
  name: "upload-upload-multipart",
  description: `Uploads a file to Cloudinary`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await uploadUploadMultipart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
