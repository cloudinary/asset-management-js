/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsRenameAsset } from "../../funcs/assetsRenameAsset.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RenameAssetRequest$inboundSchema,
};

export const tool$assetsRenameAsset: ToolDefinition<typeof args> = {
  name: "assets-rename-asset",
  description: `Renames an asset`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsRenameAsset(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
