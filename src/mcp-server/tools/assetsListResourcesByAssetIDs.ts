/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsListResourcesByAssetIDs } from "../../funcs/assetsListResourcesByAssetIDs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListResourcesByAssetIDsRequest$inboundSchema,
};

export const tool$assetsListResourcesByAssetIDs: ToolDefinition<typeof args> = {
  name: "assets-list-resources-by-asset-I-ds",
  description: `Get resources by asset IDs

Retrieves details for specific resources using their asset IDs (or external IDs).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsListResourcesByAssetIDs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
