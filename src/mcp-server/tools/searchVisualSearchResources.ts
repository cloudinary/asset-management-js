/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchVisualSearchResources } from "../../funcs/searchVisualSearchResources.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.VisualSearchParametersUnion$inboundSchema,
};

export const tool$searchVisualSearchResources: ToolDefinition<typeof args> = {
  name: "search-visual-search-resources",
  description: `Get resources by visual similarity

Returns a list of resources that are visually similar to a specified image. You can provide the source image for comparison in one of three ways:
- Provide a URL of an image
- Specify the public ID or asset ID of an existing image
- Provide a textual description
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchVisualSearchResources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
