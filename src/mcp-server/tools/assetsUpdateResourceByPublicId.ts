/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsUpdateResourceByPublicId } from "../../funcs/assetsUpdateResourceByPublicId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateResourceByPublicIdRequest$inboundSchema,
};

export const tool$assetsUpdateResourceByPublicId: ToolDefinition<typeof args> =
  {
    name: "assets-update-resource-by-public-id",
    description: `Update asset by public ID

Updates one or more attributes of a specified resource (asset) identified by its public ID. Note that you can also update many attributes of an existing asset using the explicit method, which is not rate limited.
`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await assetsUpdateResourceByPublicId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
