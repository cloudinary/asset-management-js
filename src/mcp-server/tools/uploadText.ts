/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadText } from "../../funcs/uploadText.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TextRequest$inboundSchema,
};

export const tool$uploadText: ToolDefinition<typeof args> = {
  name: "upload-text",
  description: `Create image from text

Dynamically generates an image from a specified text string.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await uploadText(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
