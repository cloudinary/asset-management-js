/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsListResourcesByAssetFolder } from "../../funcs/assetsListResourcesByAssetFolder.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListResourcesByAssetFolderRequest$inboundSchema,
};

export const tool$assetsListResourcesByAssetFolder: ToolDefinition<
  typeof args
> = {
  name: "assets-list-resources-by-asset-folder",
  description: `Get resources by asset folder

Retrieves a list of resources within a specific asset folder. Requires folder decoupling to be enabled.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsListResourcesByAssetFolder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
