/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsGetResourceByPublicId } from "../../funcs/assetsGetResourceByPublicId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourceByPublicIdRequest$inboundSchema,
};

export const tool$assetsGetResourceByPublicId: ToolDefinition<typeof args> = {
  name: "assets-get-resource-by-public-id",
  description: `Get resource by public ID

Returns the details of a single resource specified by its public ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsGetResourceByPublicId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
