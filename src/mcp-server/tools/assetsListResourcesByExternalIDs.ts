/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsListResourcesByExternalIDs } from "../../funcs/assetsListResourcesByExternalIDs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListResourcesByExternalIDsRequest$inboundSchema,
};

export const tool$assetsListResourcesByExternalIDs: ToolDefinition<
  typeof args
> = {
  name: "assets-list-resources-by-external-I-ds",
  description: `Get resources by external IDs

Retrieves details for specific resources using their external IDs (or asset IDs).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsListResourcesByExternalIDs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
