/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchSearchResourcesPost } from "../../funcs/searchSearchResourcesPost.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.SearchParameters$inboundSchema,
};

export const tool$searchSearchResourcesPost: ToolDefinition<typeof args> = {
  name: "search-search-resources-post",
  description: `Get resources by search (POST method)

Returns a list of resources matching the specified search criteria. The API supports a variety of search parameters and returns a JSON response with the matching resources and their details.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchSearchResourcesPost(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
