/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsDeleteResourcesByPublicId } from "../../funcs/assetsDeleteResourcesByPublicId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteResourcesByPublicIdRequest$inboundSchema,
};

export const tool$assetsDeleteResourcesByPublicId: ToolDefinition<typeof args> =
  {
    name: "assets-delete-resources-by-public-id",
    description: `Delete resources by public ID

Deletes assets uploaded to your product environment, identified by their public IDs.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await assetsDeleteResourcesByPublicId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
