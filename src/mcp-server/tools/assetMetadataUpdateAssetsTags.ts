/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetMetadataUpdateAssetsTags } from "../../funcs/assetMetadataUpdateAssetsTags.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  resourceType: components.ResourceType$inboundSchema,
  tagsUpdateRequest: components.TagsUpdateRequest$inboundSchema,
};

export const tool$assetMetadataUpdateAssetsTags: ToolDefinition<typeof args> = {
  name: "manage-asset-tags",
  description: `Adds, removes, or replaces tags on multiple assets

Applies a tag command to the given assets, addressing them by public ID.

The number of tags multiplied by the number of public IDs must not exceed 10,000.
`,
  scopes: ["librarian"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetMetadataUpdateAssetsTags(
      client,
      args.resourceType,
      args.tagsUpdateRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
