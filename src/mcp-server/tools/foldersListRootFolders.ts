/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { foldersListRootFolders } from "../../funcs/foldersListRootFolders.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRootFoldersRequest$inboundSchema,
};

export const tool$foldersListRootFolders: ToolDefinition<typeof args> = {
  name: "folders-list-root-folders",
  description: `Get root folders

Retrieves an array of all root folders in your Cloudinary product environment. Limited to 2000 results.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await foldersListRootFolders(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
