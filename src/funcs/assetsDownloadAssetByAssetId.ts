/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { CloudinaryAssetMgmtCore } from "../core.js";
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { CloudinaryAssetMgmtError } from "../models/errors/cloudinaryassetmgmterror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

export enum DownloadAssetByAssetIdAcceptEnum {
  applicationOctetStream = "application/octet-stream",
  videoWildcard = "video/*",
  imageWildcard = "image/*",
}

/**
 * Download an asset by asset ID
 *
 * @remarks
 * Generates a download link for a specific asset identified by its immutable asset ID,
 * instead of the resource type, delivery type and public ID.
 */
export function assetsDownloadAssetByAssetId(
  client: CloudinaryAssetMgmtCore,
  assetId: string,
  format?: string | undefined,
  transformation?: string | undefined,
  attachment?: boolean | undefined,
  targetFilename?: string | undefined,
  streamingAttachment?: boolean | undefined,
  expiresAt?: number | undefined,
  options?: RequestOptions & {
    acceptHeaderOverride?: DownloadAssetByAssetIdAcceptEnum;
  },
): APIPromise<
  Result<
    operations.DownloadAssetByAssetIdResponse,
    | errors.ApiError
    | CloudinaryAssetMgmtError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    assetId,
    format,
    transformation,
    attachment,
    targetFilename,
    streamingAttachment,
    expiresAt,
    options,
  ));
}

async function $do(
  client: CloudinaryAssetMgmtCore,
  assetId: string,
  format?: string | undefined,
  transformation?: string | undefined,
  attachment?: boolean | undefined,
  targetFilename?: string | undefined,
  streamingAttachment?: boolean | undefined,
  expiresAt?: number | undefined,
  options?: RequestOptions & {
    acceptHeaderOverride?: DownloadAssetByAssetIdAcceptEnum;
  },
): Promise<
  [
    Result<
      operations.DownloadAssetByAssetIdResponse,
      | errors.ApiError
      | CloudinaryAssetMgmtError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: operations.DownloadAssetByAssetIdRequest = {
    assetId: assetId,
    format: format,
    transformation: transformation,
    attachment: attachment,
    targetFilename: targetFilename,
    streamingAttachment: streamingAttachment,
    expiresAt: expiresAt,
  };

  const parsed = safeParse(
    input,
    (value) =>
      operations.DownloadAssetByAssetIdRequest$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const pathParams = {
    cloud_name: encodeSimple("cloud_name", client._options.cloudName, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/v1_1/{cloud_name}/asset/download")(pathParams);

  const query = encodeFormQuery({
    "asset_id": payload.asset_id,
    "attachment": payload.attachment,
    "expires_at": payload.expires_at,
    "format": payload.format,
    "streaming_attachment": payload.streaming_attachment,
    "target_filename": payload.target_filename,
    "transformation": payload.transformation,
  });

  const headers = new Headers(compactMap({
    Accept: options?.acceptHeaderOverride
      || "application/octet-stream;q=1, video/*;q=0.7, image/*;q=0",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "downloadAssetByAssetId",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.DownloadAssetByAssetIdResponse,
    | errors.ApiError
    | CloudinaryAssetMgmtError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.stream(200, operations.DownloadAssetByAssetIdResponse$inboundSchema),
    M.stream(200, operations.DownloadAssetByAssetIdResponse$inboundSchema, {
      ctype: "image/*",
    }),
    M.stream(200, operations.DownloadAssetByAssetIdResponse$inboundSchema, {
      ctype: "video/*",
    }),
    M.jsonErr([400, 401, 403, 404], errors.ApiError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
