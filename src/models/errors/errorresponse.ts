/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import * as components from "../components/index.js";
import { CloudinaryAssetMgmtError } from "./cloudinaryassetmgmterror.js";

/**
 * Wrapper for error responses; includes the error object and a request_id for correlation.
 */
export type ErrorResponseData = {
  /**
   * Details of an error, including a coarse category for retry logic, a stable error code, and a human-readable message.
   */
  error: components.ErrorT;
  /**
   * Unique identifier for this request, for correlation and support.
   */
  requestId: string;
};

/**
 * Wrapper for error responses; includes the error object and a request_id for correlation.
 */
export class ErrorResponse extends CloudinaryAssetMgmtError {
  /**
   * Details of an error, including a coarse category for retry logic, a stable error code, and a human-readable message.
   */
  error: components.ErrorT;
  /**
   * Unique identifier for this request, for correlation and support.
   */
  requestId: string;

  /** The original data that was passed to this error instance. */
  data$: ErrorResponseData;

  constructor(
    err: ErrorResponseData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.error?.message
      || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.error = err.error;
    this.requestId = err.requestId;

    this.name = "ErrorResponse";
  }
}

/** @internal */
export const ErrorResponse$inboundSchema: z.ZodType<
  ErrorResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  error: components.ErrorT$inboundSchema,
  request_id: z.string(),
  request$: z.instanceof(Request),
  response$: z.instanceof(Response),
  body$: z.string(),
})
  .transform((v) => {
    const remapped = remap$(v, {
      "request_id": "requestId",
    });

    return new ErrorResponse(remapped, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });

/** @internal */
export type ErrorResponse$Outbound = {
  error: components.ErrorT$Outbound;
  request_id: string;
};

/** @internal */
export const ErrorResponse$outboundSchema: z.ZodType<
  ErrorResponse$Outbound,
  z.ZodTypeDef,
  ErrorResponse
> = z.instanceof(ErrorResponse)
  .transform(v => v.data$)
  .pipe(
    z.object({
      error: components.ErrorT$outboundSchema,
      requestId: z.string(),
    }).transform((v) => {
      return remap$(v, {
        requestId: "request_id",
      });
    }),
  );
