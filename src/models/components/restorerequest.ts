/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RestoreRequest = {
  /**
   * The unique and immutable asset IDs of backed up assets to restore.
   */
  assetIds: Array<string>;
  /**
   * If you specify versions, the number of versions in the array must exactly match the number of asset_ids.
   */
  versions?: Array<string> | undefined;
  /**
   * The URL that will receive notification when restore is complete.
   */
  notificationUrl?: string | undefined;
};

/** @internal */
export const RestoreRequest$inboundSchema: z.ZodType<
  RestoreRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  asset_ids: z.array(z.string()),
  versions: z.array(z.string()).optional(),
  notification_url: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "asset_ids": "assetIds",
    "notification_url": "notificationUrl",
  });
});
/** @internal */
export type RestoreRequest$Outbound = {
  asset_ids: Array<string>;
  versions?: Array<string> | undefined;
  notification_url?: string | undefined;
};

/** @internal */
export const RestoreRequest$outboundSchema: z.ZodType<
  RestoreRequest$Outbound,
  z.ZodTypeDef,
  RestoreRequest
> = z.object({
  assetIds: z.array(z.string()),
  versions: z.array(z.string()).optional(),
  notificationUrl: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    assetIds: "asset_ids",
    notificationUrl: "notification_url",
  });
});

export function restoreRequestToJSON(restoreRequest: RestoreRequest): string {
  return JSON.stringify(RestoreRequest$outboundSchema.parse(restoreRequest));
}
export function restoreRequestFromJSON(
  jsonString: string,
): SafeParseResult<RestoreRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RestoreRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RestoreRequest' from JSON`,
  );
}
