/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  FolderRole,
  FolderRole$inboundSchema,
  FolderRole$Outbound,
  FolderRole$outboundSchema,
} from "./folderrole.js";
import {
  PrincipalType,
  PrincipalType$inboundSchema,
  PrincipalType$outboundSchema,
} from "./principaltype.js";

export type FolderRoleAssignment = {
  /**
   * The type of principal.
   */
  type: PrincipalType;
  /**
   * The unique identifier of the principal. For `apiKey`, provide the API key value.
   */
  id: string;
  /**
   * The ID of the folder where this role assignment was defined, either the requested folder or one of its ancestor folders.
   */
  folderId: string;
  /**
   * The roles assigned to this principal for this folder.
   */
  roles: Array<FolderRole>;
};

/** @internal */
export const FolderRoleAssignment$inboundSchema: z.ZodType<
  FolderRoleAssignment,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: PrincipalType$inboundSchema,
  id: z.string(),
  folder_id: z.string(),
  roles: z.array(FolderRole$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "folder_id": "folderId",
  });
});
/** @internal */
export type FolderRoleAssignment$Outbound = {
  type: string;
  id: string;
  folder_id: string;
  roles: Array<FolderRole$Outbound>;
};

/** @internal */
export const FolderRoleAssignment$outboundSchema: z.ZodType<
  FolderRoleAssignment$Outbound,
  z.ZodTypeDef,
  FolderRoleAssignment
> = z.object({
  type: PrincipalType$outboundSchema,
  id: z.string(),
  folderId: z.string(),
  roles: z.array(FolderRole$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    folderId: "folder_id",
  });
});

export function folderRoleAssignmentToJSON(
  folderRoleAssignment: FolderRoleAssignment,
): string {
  return JSON.stringify(
    FolderRoleAssignment$outboundSchema.parse(folderRoleAssignment),
  );
}
export function folderRoleAssignmentFromJSON(
  jsonString: string,
): SafeParseResult<FolderRoleAssignment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FolderRoleAssignment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FolderRoleAssignment' from JSON`,
  );
}
