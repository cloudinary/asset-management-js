/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  InitialBackupStatusEnum,
  InitialBackupStatusEnum$inboundSchema,
  InitialBackupStatusEnum$outboundSchema,
} from "./initialbackupstatusenum.js";

export type InitialBackupSummary = {
  /**
   * The unique identifier of the initial backup operation.
   */
  id: string;
  /**
   * The current status of the initial backup operation.
   */
  status: InitialBackupStatusEnum;
  /**
   * The date and time when the initial backup operation was created.
   */
  createdAt: Date;
};

/** @internal */
export const InitialBackupSummary$inboundSchema: z.ZodType<
  InitialBackupSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  status: InitialBackupStatusEnum$inboundSchema,
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
  });
});
/** @internal */
export type InitialBackupSummary$Outbound = {
  id: string;
  status: string;
  created_at: string;
};

/** @internal */
export const InitialBackupSummary$outboundSchema: z.ZodType<
  InitialBackupSummary$Outbound,
  z.ZodTypeDef,
  InitialBackupSummary
> = z.object({
  id: z.string(),
  status: InitialBackupStatusEnum$outboundSchema,
  createdAt: z.date().transform(v => v.toISOString()),
}).transform((v) => {
  return remap$(v, {
    createdAt: "created_at",
  });
});

export function initialBackupSummaryToJSON(
  initialBackupSummary: InitialBackupSummary,
): string {
  return JSON.stringify(
    InitialBackupSummary$outboundSchema.parse(initialBackupSummary),
  );
}
export function initialBackupSummaryFromJSON(
  jsonString: string,
): SafeParseResult<InitialBackupSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InitialBackupSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InitialBackupSummary' from JSON`,
  );
}
