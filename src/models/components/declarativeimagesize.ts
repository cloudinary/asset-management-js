/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Output aspect ratio, width-to-height. Limited to the core ratios
 *
 * @remarks
 * every model supports natively.
 */
export const AspectRatio = {
  Eleven: "1:1",
  OneHundredAndSixtyNine: "16:9",
  NineHundredAndSixteen: "9:16",
  FortyThree: "4:3",
  ThirtyFour: "3:4",
} as const;
/**
 * Output aspect ratio, width-to-height. Limited to the core ratios
 *
 * @remarks
 * every model supports natively.
 */
export type AspectRatio = ClosedEnum<typeof AspectRatio>;

/**
 * Output resolution tier, measured on the longest edge. Defaults to
 *
 * @remarks
 * `1K` when omitted.
 * * `0.5K`: ~512 px
 * * `1K`: ~1024 px
 * * `2K`: ~2048 px
 * * `4K`: ~4096 px
 */
export const Resolution = {
  ZeroDot5K: "0.5K",
  OneK: "1K",
  TwoK: "2K",
  FourK: "4K",
} as const;
/**
 * Output resolution tier, measured on the longest edge. Defaults to
 *
 * @remarks
 * `1K` when omitted.
 * * `0.5K`: ~512 px
 * * `1K`: ~1024 px
 * * `2K`: ~2048 px
 * * `4K`: ~4096 px
 */
export type Resolution = ClosedEnum<typeof Resolution>;

/**
 * Declarative output size — an aspect ratio plus an optional resolution
 *
 * @remarks
 * tier, resolved server-side to the nearest size the model supports.
 */
export type DeclarativeImageSize = {
  /**
   * Output aspect ratio, width-to-height. Limited to the core ratios
   *
   * @remarks
   * every model supports natively.
   */
  aspectRatio: AspectRatio;
  /**
   * Output resolution tier, measured on the longest edge. Defaults to
   *
   * @remarks
   * `1K` when omitted.
   * * `0.5K`: ~512 px
   * * `1K`: ~1024 px
   * * `2K`: ~2048 px
   * * `4K`: ~4096 px
   */
  resolution?: Resolution | undefined;
};

/** @internal */
export const AspectRatio$inboundSchema: z.ZodNativeEnum<typeof AspectRatio> = z
  .nativeEnum(AspectRatio);
/** @internal */
export const AspectRatio$outboundSchema: z.ZodNativeEnum<typeof AspectRatio> =
  AspectRatio$inboundSchema;

/** @internal */
export const Resolution$inboundSchema: z.ZodNativeEnum<typeof Resolution> = z
  .nativeEnum(Resolution);
/** @internal */
export const Resolution$outboundSchema: z.ZodNativeEnum<typeof Resolution> =
  Resolution$inboundSchema;

/** @internal */
export const DeclarativeImageSize$inboundSchema: z.ZodType<
  DeclarativeImageSize,
  z.ZodTypeDef,
  unknown
> = z.object({
  aspect_ratio: AspectRatio$inboundSchema,
  resolution: Resolution$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "aspect_ratio": "aspectRatio",
  });
});
/** @internal */
export type DeclarativeImageSize$Outbound = {
  aspect_ratio: string;
  resolution?: string | undefined;
};

/** @internal */
export const DeclarativeImageSize$outboundSchema: z.ZodType<
  DeclarativeImageSize$Outbound,
  z.ZodTypeDef,
  DeclarativeImageSize
> = z.object({
  aspectRatio: AspectRatio$outboundSchema,
  resolution: Resolution$outboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    aspectRatio: "aspect_ratio",
  });
});

export function declarativeImageSizeToJSON(
  declarativeImageSize: DeclarativeImageSize,
): string {
  return JSON.stringify(
    DeclarativeImageSize$outboundSchema.parse(declarativeImageSize),
  );
}
export function declarativeImageSizeFromJSON(
  jsonString: string,
): SafeParseResult<DeclarativeImageSize, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeclarativeImageSize$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeclarativeImageSize' from JSON`,
  );
}
