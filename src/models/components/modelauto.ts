/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The only valid value; selects automatic model selection.
 */
export const Mode = {
  Auto: "auto",
} as const;
/**
 * The only valid value; selects automatic model selection.
 */
export type Mode = ClosedEnum<typeof Mode>;

/**
 * What the choice optimizes for:
 *
 * @remarks
 * * `balanced` (default): quality and cost weighted equally.
 * * `quality`: quality only; cost isn't weighed.
 * * `economy`: cost first, quality second.
 * * `balanced_fast`, `quality_fast`, `economy_fast`: as above, but also
 *
 *   favor faster generation, and never pick a model that typically
 *   takes longer than 30 seconds.
 */
export const Preference = {
  Balanced: "balanced",
  Quality: "quality",
  Economy: "economy",
  BalancedFast: "balanced_fast",
  QualityFast: "quality_fast",
  EconomyFast: "economy_fast",
} as const;
/**
 * What the choice optimizes for:
 *
 * @remarks
 * * `balanced` (default): quality and cost weighted equally.
 * * `quality`: quality only; cost isn't weighed.
 * * `economy`: cost first, quality second.
 * * `balanced_fast`, `quality_fast`, `economy_fast`: as above, but also
 *
 *   favor faster generation, and never pick a model that typically
 *   takes longer than 30 seconds.
 */
export type Preference = ClosedEnum<typeof Preference>;

/**
 * Let the service choose the model for this request. The response reports
 *
 * @remarks
 * the resolved model in `data.assets[].model`, as for the other forms; on
 * `image_to_image` it is always an edit model.
 */
export type ModelAuto = {
  /**
   * The only valid value; selects automatic model selection.
   */
  mode: Mode;
  /**
   * What the choice optimizes for:
   *
   * @remarks
   * * `balanced` (default): quality and cost weighted equally.
   * * `quality`: quality only; cost isn't weighed.
   * * `economy`: cost first, quality second.
   * * `balanced_fast`, `quality_fast`, `economy_fast`: as above, but also
   *
   *   favor faster generation, and never pick a model that typically
   *   takes longer than 30 seconds.
   */
  preference?: Preference | undefined;
};

/** @internal */
export const Mode$inboundSchema: z.ZodNativeEnum<typeof Mode> = z.nativeEnum(
  Mode,
);
/** @internal */
export const Mode$outboundSchema: z.ZodNativeEnum<typeof Mode> =
  Mode$inboundSchema;

/** @internal */
export const Preference$inboundSchema: z.ZodNativeEnum<typeof Preference> = z
  .nativeEnum(Preference);
/** @internal */
export const Preference$outboundSchema: z.ZodNativeEnum<typeof Preference> =
  Preference$inboundSchema;

/** @internal */
export const ModelAuto$inboundSchema: z.ZodType<
  ModelAuto,
  z.ZodTypeDef,
  unknown
> = z.object({
  mode: Mode$inboundSchema,
  preference: Preference$inboundSchema.default("balanced"),
});
/** @internal */
export type ModelAuto$Outbound = {
  mode: string;
  preference: string;
};

/** @internal */
export const ModelAuto$outboundSchema: z.ZodType<
  ModelAuto$Outbound,
  z.ZodTypeDef,
  ModelAuto
> = z.object({
  mode: Mode$outboundSchema,
  preference: Preference$outboundSchema.default("balanced"),
});

export function modelAutoToJSON(modelAuto: ModelAuto): string {
  return JSON.stringify(ModelAuto$outboundSchema.parse(modelAuto));
}
export function modelAutoFromJSON(
  jsonString: string,
): SafeParseResult<ModelAuto, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ModelAuto$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ModelAuto' from JSON`,
  );
}
