/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  Feature,
  Feature$inboundSchema,
  Feature$outboundSchema,
} from "./feature.js";

/**
 * Quota usage for a single add-on.
 */
export type AddonQuota = {
  /**
   * The add-on a quota applies to.
   */
  type: Feature;
  /**
   * Number of generations consumed by this request.
   */
  usedByRequest: number | null;
  /**
   * Generations remaining in the current period.
   */
  remaining: number | null;
  /**
   * Maximum generations allowed per period.
   */
  limit: number | null;
};

/** @internal */
export const AddonQuota$inboundSchema: z.ZodType<
  AddonQuota,
  z.ZodTypeDef,
  unknown
> = z.object({
  type: Feature$inboundSchema,
  used_by_request: z.nullable(z.number().int()),
  remaining: z.nullable(z.number().int()),
  limit: z.nullable(z.number().int()),
}).transform((v) => {
  return remap$(v, {
    "used_by_request": "usedByRequest",
  });
});
/** @internal */
export type AddonQuota$Outbound = {
  type: string;
  used_by_request: number | null;
  remaining: number | null;
  limit: number | null;
};

/** @internal */
export const AddonQuota$outboundSchema: z.ZodType<
  AddonQuota$Outbound,
  z.ZodTypeDef,
  AddonQuota
> = z.object({
  type: Feature$outboundSchema,
  usedByRequest: z.nullable(z.number().int()),
  remaining: z.nullable(z.number().int()),
  limit: z.nullable(z.number().int()),
}).transform((v) => {
  return remap$(v, {
    usedByRequest: "used_by_request",
  });
});

export function addonQuotaToJSON(addonQuota: AddonQuota): string {
  return JSON.stringify(AddonQuota$outboundSchema.parse(addonQuota));
}
export function addonQuotaFromJSON(
  jsonString: string,
): SafeParseResult<AddonQuota, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AddonQuota$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AddonQuota' from JSON`,
  );
}
