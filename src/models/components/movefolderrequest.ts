/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The folder move/rename request.
 */
export type MoveFolderRequest = {
  /**
   * The new path for the folder.
   */
  toFolder: string;
};

/** @internal */
export const MoveFolderRequest$inboundSchema: z.ZodType<
  MoveFolderRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  to_folder: z.string(),
}).transform((v) => {
  return remap$(v, {
    "to_folder": "toFolder",
  });
});
/** @internal */
export type MoveFolderRequest$Outbound = {
  to_folder: string;
};

/** @internal */
export const MoveFolderRequest$outboundSchema: z.ZodType<
  MoveFolderRequest$Outbound,
  z.ZodTypeDef,
  MoveFolderRequest
> = z.object({
  toFolder: z.string(),
}).transform((v) => {
  return remap$(v, {
    toFolder: "to_folder",
  });
});

export function moveFolderRequestToJSON(
  moveFolderRequest: MoveFolderRequest,
): string {
  return JSON.stringify(
    MoveFolderRequest$outboundSchema.parse(moveFolderRequest),
  );
}
export function moveFolderRequestFromJSON(
  jsonString: string,
): SafeParseResult<MoveFolderRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MoveFolderRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MoveFolderRequest' from JSON`,
  );
}
