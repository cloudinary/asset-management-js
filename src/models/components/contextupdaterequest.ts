/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DeliveryTypeAll,
  DeliveryTypeAll$inboundSchema,
  DeliveryTypeAll$Outbound,
  DeliveryTypeAll$outboundSchema,
} from "./deliverytypeall.js";

/**
 * The contextual-metadata operation to apply.
 *
 * @remarks
 *
 * - `add` merges the given key-value pairs into each asset's existing context. Keys
 *   already present are overwritten; keys not mentioned are left intact. `context`
 *   is required.
 * - `remove_all` clears all contextual metadata. The `context` field is ignored.
 */
export const ContextUpdateRequestCommand = {
  Add: "add",
  RemoveAll: "remove_all",
} as const;
/**
 * The contextual-metadata operation to apply.
 *
 * @remarks
 *
 * - `add` merges the given key-value pairs into each asset's existing context. Keys
 *   already present are overwritten; keys not mentioned are left intact. `context`
 *   is required.
 * - `remove_all` clears all contextual metadata. The `context` field is ignored.
 */
export type ContextUpdateRequestCommand = ClosedEnum<
  typeof ContextUpdateRequestCommand
>;

/**
 * A contextual-metadata command plus the assets it applies to.
 */
export type ContextUpdateRequest = {
  /**
   * The public IDs of the assets to apply the operation to. At most 1000 are processed per request; any beyond the first 1000 are ignored rather than rejected. Public IDs that do not exist, or that the caller lacks update permission for, are skipped rather than failing the request, so the returned `public_ids` may be shorter than the requested list.
   */
  publicIds: Array<string>;
  /**
   * The delivery type of the assets to operate on. Defaults to `upload`.
   */
  type?: DeliveryTypeAll | undefined;
  /**
   * The contextual-metadata operation to apply.
   *
   * @remarks
   *
   * - `add` merges the given key-value pairs into each asset's existing context. Keys
   *   already present are overwritten; keys not mentioned are left intact. `context`
   *   is required.
   * - `remove_all` clears all contextual metadata. The `context` field is ignored.
   */
  command: ContextUpdateRequestCommand;
  /**
   * The key-value pairs to merge. Required for `add`; ignored for `remove_all`.
   *
   * @remarks
   * An empty value deletes that key rather than storing an empty string, so `"alt="` removes `alt` while leaving other keys intact. Deletes and sets may be combined: `"alt=|owner=new"`.
   * A literal `=` or `|` inside a value must be escaped with a prepending backslash, and a literal backslash must be doubled. A double quote must not be escaped.
   */
  context?: string | undefined;
};

/** @internal */
export const ContextUpdateRequestCommand$inboundSchema: z.ZodNativeEnum<
  typeof ContextUpdateRequestCommand
> = z.nativeEnum(ContextUpdateRequestCommand);
/** @internal */
export const ContextUpdateRequestCommand$outboundSchema: z.ZodNativeEnum<
  typeof ContextUpdateRequestCommand
> = ContextUpdateRequestCommand$inboundSchema;

/** @internal */
export const ContextUpdateRequest$inboundSchema: z.ZodType<
  ContextUpdateRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  public_ids: z.array(z.string()),
  type: DeliveryTypeAll$inboundSchema.optional(),
  command: ContextUpdateRequestCommand$inboundSchema,
  context: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "public_ids": "publicIds",
  });
});
/** @internal */
export type ContextUpdateRequest$Outbound = {
  public_ids: Array<string>;
  type?: DeliveryTypeAll$Outbound | undefined;
  command: string;
  context?: string | undefined;
};

/** @internal */
export const ContextUpdateRequest$outboundSchema: z.ZodType<
  ContextUpdateRequest$Outbound,
  z.ZodTypeDef,
  ContextUpdateRequest
> = z.object({
  publicIds: z.array(z.string()),
  type: DeliveryTypeAll$outboundSchema.optional(),
  command: ContextUpdateRequestCommand$outboundSchema,
  context: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    publicIds: "public_ids",
  });
});

export function contextUpdateRequestToJSON(
  contextUpdateRequest: ContextUpdateRequest,
): string {
  return JSON.stringify(
    ContextUpdateRequest$outboundSchema.parse(contextUpdateRequest),
  );
}
export function contextUpdateRequestFromJSON(
  jsonString: string,
): SafeParseResult<ContextUpdateRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextUpdateRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextUpdateRequest' from JSON`,
  );
}
