/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A generated asset stored as a short-lived asset on the upload service.
 */
export type TemporaryStorage = {
  /**
   * Discriminator identifying this as temporary storage.
   */
  storageType: "temporary";
  /**
   * The URL to fetch the generated asset.
   */
  secureUrl: string;
  /**
   * Time in UTC when secure_url stops being valid.
   */
  expiresAt: Date;
};

/** @internal */
export const TemporaryStorage$inboundSchema: z.ZodType<
  TemporaryStorage,
  z.ZodTypeDef,
  unknown
> = z.object({
  storage_type: z.literal("temporary"),
  secure_url: z.string(),
  expires_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "storage_type": "storageType",
    "secure_url": "secureUrl",
    "expires_at": "expiresAt",
  });
});
/** @internal */
export type TemporaryStorage$Outbound = {
  storage_type: "temporary";
  secure_url: string;
  expires_at: string;
};

/** @internal */
export const TemporaryStorage$outboundSchema: z.ZodType<
  TemporaryStorage$Outbound,
  z.ZodTypeDef,
  TemporaryStorage
> = z.object({
  storageType: z.literal("temporary"),
  secureUrl: z.string(),
  expiresAt: z.date().transform(v => v.toISOString()),
}).transform((v) => {
  return remap$(v, {
    storageType: "storage_type",
    secureUrl: "secure_url",
    expiresAt: "expires_at",
  });
});

export function temporaryStorageToJSON(
  temporaryStorage: TemporaryStorage,
): string {
  return JSON.stringify(
    TemporaryStorage$outboundSchema.parse(temporaryStorage),
  );
}
export function temporaryStorageFromJSON(
  jsonString: string,
): SafeParseResult<TemporaryStorage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TemporaryStorage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TemporaryStorage' from JSON`,
  );
}
