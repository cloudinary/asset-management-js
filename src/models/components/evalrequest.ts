/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The eval expression and the upload parameters to evaluate it against. The `eval` field holds
 *
 * @remarks
 * the dynamic expression; any other upload parameters may be supplied alongside it.
 */
export type EvalRequest = {
  /**
   * The dynamic eval expression to evaluate against the upload parameters.
   */
  eval: string;
  /**
   * The public ID of the asset the eval expression applies to.
   */
  publicId?: string | undefined;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const EvalRequest$inboundSchema: z.ZodType<
  EvalRequest,
  z.ZodTypeDef,
  unknown
> = collectExtraKeys$(
  z.object({
    eval: z.string(),
    public_id: z.string().optional(),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "public_id": "publicId",
  });
});
/** @internal */
export type EvalRequest$Outbound = {
  eval: string;
  public_id?: string | undefined;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const EvalRequest$outboundSchema: z.ZodType<
  EvalRequest$Outbound,
  z.ZodTypeDef,
  EvalRequest
> = z.object({
  eval: z.string(),
  publicId: z.string().optional(),
  additionalProperties: z.record(z.any()).optional(),
}).transform((v) => {
  return {
    ...v.additionalProperties,
    ...remap$(v, {
      publicId: "public_id",
      additionalProperties: null,
    }),
  };
});

export function evalRequestToJSON(evalRequest: EvalRequest): string {
  return JSON.stringify(EvalRequest$outboundSchema.parse(evalRequest));
}
export function evalRequestFromJSON(
  jsonString: string,
): SafeParseResult<EvalRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EvalRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EvalRequest' from JSON`,
  );
}
