/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The items to inspect for a type and the actions to check against them.
 */
export type PeopleInspectionGroup = {
  /**
   * A map of caller-supplied cache keys to the person identifier to inspect.
   */
  items: { [k: string]: any };
  /**
   * A map of action names to optional action context to inspect.
   */
  actions: { [k: string]: any };
};

/** @internal */
export const PeopleInspectionGroup$inboundSchema: z.ZodType<
  PeopleInspectionGroup,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.record(z.any()),
  actions: z.record(z.any()),
});
/** @internal */
export type PeopleInspectionGroup$Outbound = {
  items: { [k: string]: any };
  actions: { [k: string]: any };
};

/** @internal */
export const PeopleInspectionGroup$outboundSchema: z.ZodType<
  PeopleInspectionGroup$Outbound,
  z.ZodTypeDef,
  PeopleInspectionGroup
> = z.object({
  items: z.record(z.any()),
  actions: z.record(z.any()),
});

export function peopleInspectionGroupToJSON(
  peopleInspectionGroup: PeopleInspectionGroup,
): string {
  return JSON.stringify(
    PeopleInspectionGroup$outboundSchema.parse(peopleInspectionGroup),
  );
}
export function peopleInspectionGroupFromJSON(
  jsonString: string,
): SafeParseResult<PeopleInspectionGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PeopleInspectionGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PeopleInspectionGroup' from JSON`,
  );
}
