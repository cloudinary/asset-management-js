/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type PermittedRole = {
  /**
   * The role identifier.
   */
  id: string;
  /**
   * The human-readable name of the role.
   */
  name: string;
  /**
   * A description of the role's permissions.
   */
  description: string;
};

/** @internal */
export const PermittedRole$inboundSchema: z.ZodType<
  PermittedRole,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  name: z.string(),
  description: z.string(),
});
/** @internal */
export type PermittedRole$Outbound = {
  id: string;
  name: string;
  description: string;
};

/** @internal */
export const PermittedRole$outboundSchema: z.ZodType<
  PermittedRole$Outbound,
  z.ZodTypeDef,
  PermittedRole
> = z.object({
  id: z.string(),
  name: z.string(),
  description: z.string(),
});

export function permittedRoleToJSON(permittedRole: PermittedRole): string {
  return JSON.stringify(PermittedRole$outboundSchema.parse(permittedRole));
}
export function permittedRoleFromJSON(
  jsonString: string,
): SafeParseResult<PermittedRole, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PermittedRole$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PermittedRole' from JSON`,
  );
}
