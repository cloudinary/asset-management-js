/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The result of a bulk operation addressed by public ID.
 */
export type PublicIdsResponse = {
  /**
   * The public IDs the operation was actually applied to. Shorter than the requested list when assets were missing, unauthorized, or beyond the 1000-asset limit.
   */
  publicIds?: Array<string> | undefined;
  /**
   * The public IDs the caller lacks update permission for. Present only when at least one requested asset was unauthorized.
   */
  unauthorized?: Array<string> | undefined;
};

/** @internal */
export const PublicIdsResponse$inboundSchema: z.ZodType<
  PublicIdsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  public_ids: z.array(z.string()).optional(),
  unauthorized: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "public_ids": "publicIds",
  });
});
/** @internal */
export type PublicIdsResponse$Outbound = {
  public_ids?: Array<string> | undefined;
  unauthorized?: Array<string> | undefined;
};

/** @internal */
export const PublicIdsResponse$outboundSchema: z.ZodType<
  PublicIdsResponse$Outbound,
  z.ZodTypeDef,
  PublicIdsResponse
> = z.object({
  publicIds: z.array(z.string()).optional(),
  unauthorized: z.array(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    publicIds: "public_ids",
  });
});

export function publicIdsResponseToJSON(
  publicIdsResponse: PublicIdsResponse,
): string {
  return JSON.stringify(
    PublicIdsResponse$outboundSchema.parse(publicIdsResponse),
  );
}
export function publicIdsResponseFromJSON(
  jsonString: string,
): SafeParseResult<PublicIdsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicIdsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicIdsResponse' from JSON`,
  );
}
