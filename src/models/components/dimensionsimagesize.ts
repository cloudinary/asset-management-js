/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Exact output dimensions in pixels. Both `width` and `height` are required together.
 */
export type DimensionsImageSize = {
  /**
   * Output width in pixels.
   */
  width: number;
  /**
   * Output height in pixels.
   */
  height: number;
};

/** @internal */
export const DimensionsImageSize$inboundSchema: z.ZodType<
  DimensionsImageSize,
  z.ZodTypeDef,
  unknown
> = z.object({
  width: z.number().int(),
  height: z.number().int(),
});
/** @internal */
export type DimensionsImageSize$Outbound = {
  width: number;
  height: number;
};

/** @internal */
export const DimensionsImageSize$outboundSchema: z.ZodType<
  DimensionsImageSize$Outbound,
  z.ZodTypeDef,
  DimensionsImageSize
> = z.object({
  width: z.number().int(),
  height: z.number().int(),
});

export function dimensionsImageSizeToJSON(
  dimensionsImageSize: DimensionsImageSize,
): string {
  return JSON.stringify(
    DimensionsImageSize$outboundSchema.parse(dimensionsImageSize),
  );
}
export function dimensionsImageSizeFromJSON(
  jsonString: string,
): SafeParseResult<DimensionsImageSize, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DimensionsImageSize$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DimensionsImageSize' from JSON`,
  );
}
