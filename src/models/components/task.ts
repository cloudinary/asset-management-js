/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  GeneratedAsset,
  GeneratedAsset$inboundSchema,
  GeneratedAsset$Outbound,
  GeneratedAsset$outboundSchema,
} from "./generatedasset.js";
import {
  Limits,
  Limits$inboundSchema,
  Limits$Outbound,
  Limits$outboundSchema,
} from "./limits.js";
import {
  TaskStatus,
  TaskStatus$inboundSchema,
  TaskStatus$outboundSchema,
} from "./taskstatus.js";

/**
 * The generated assets. Populated once `status` is `completed`.
 *
 * @remarks
 */
export type TaskResult = {
  /**
   * The generated assets. Currently a single asset is returned.
   */
  assets: Array<GeneratedAsset>;
};

/**
 * An async generation task. Returned when a generation is accepted (202)
 *
 * @remarks
 * and from GET /tasks/{task_id} as it progresses. The `result` is filled
 * in once `status` is `completed`.
 */
export type Task = {
  /**
   * Identifier for the async generation task. Use it to poll `GET /generate/{cloud_name}/tasks/{task_id}`.
   */
  taskId: string;
  /**
   * The status of an async generation task.
   *
   * @remarks
   * * `pending`: accepted and queued, not yet started.
   * * `processing`: generation is in progress.
   * * `completed`: generation finished; `result` is populated.
   * * `failed`: generation did not complete successfully.
   */
  status: TaskStatus;
  /**
   * The generated assets. Populated once `status` is `completed`.
   *
   * @remarks
   */
  result?: TaskResult | null | undefined;
  /**
   * Rate limit information for the account's add-on quotas.
   */
  limits?: Limits | undefined;
};

/** @internal */
export const TaskResult$inboundSchema: z.ZodType<
  TaskResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  assets: z.array(GeneratedAsset$inboundSchema),
});
/** @internal */
export type TaskResult$Outbound = {
  assets: Array<GeneratedAsset$Outbound>;
};

/** @internal */
export const TaskResult$outboundSchema: z.ZodType<
  TaskResult$Outbound,
  z.ZodTypeDef,
  TaskResult
> = z.object({
  assets: z.array(GeneratedAsset$outboundSchema),
});

export function taskResultToJSON(taskResult: TaskResult): string {
  return JSON.stringify(TaskResult$outboundSchema.parse(taskResult));
}
export function taskResultFromJSON(
  jsonString: string,
): SafeParseResult<TaskResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskResult' from JSON`,
  );
}

/** @internal */
export const Task$inboundSchema: z.ZodType<Task, z.ZodTypeDef, unknown> = z
  .object({
    task_id: z.string(),
    status: TaskStatus$inboundSchema,
    result: z.nullable(z.lazy(() => TaskResult$inboundSchema)).optional(),
    limits: Limits$inboundSchema.optional(),
  }).transform((v) => {
    return remap$(v, {
      "task_id": "taskId",
    });
  });
/** @internal */
export type Task$Outbound = {
  task_id: string;
  status: string;
  result?: TaskResult$Outbound | null | undefined;
  limits?: Limits$Outbound | undefined;
};

/** @internal */
export const Task$outboundSchema: z.ZodType<Task$Outbound, z.ZodTypeDef, Task> =
  z.object({
    taskId: z.string(),
    status: TaskStatus$outboundSchema,
    result: z.nullable(z.lazy(() => TaskResult$outboundSchema)).optional(),
    limits: Limits$outboundSchema.optional(),
  }).transform((v) => {
    return remap$(v, {
      taskId: "task_id",
    });
  });

export function taskToJSON(task: Task): string {
  return JSON.stringify(Task$outboundSchema.parse(task));
}
export function taskFromJSON(
  jsonString: string,
): SafeParseResult<Task, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Task$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Task' from JSON`,
  );
}
