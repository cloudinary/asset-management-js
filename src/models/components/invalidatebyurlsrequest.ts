/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The set of delivery URLs whose derived assets should be invalidated.
 */
export type InvalidateByUrlsRequest = {
  /**
   * The delivery URLs whose cached derived assets should be invalidated.
   */
  urls: Array<string>;
  /**
   * Whether to skip recording this invalidation against the product environment usage. If false, the invalidation is counted towards usage. Default is false.
   */
  skipReporting?: boolean | undefined;
};

/** @internal */
export const InvalidateByUrlsRequest$inboundSchema: z.ZodType<
  InvalidateByUrlsRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  urls: z.array(z.string()),
  skip_reporting: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    "skip_reporting": "skipReporting",
  });
});
/** @internal */
export type InvalidateByUrlsRequest$Outbound = {
  urls: Array<string>;
  skip_reporting?: boolean | undefined;
};

/** @internal */
export const InvalidateByUrlsRequest$outboundSchema: z.ZodType<
  InvalidateByUrlsRequest$Outbound,
  z.ZodTypeDef,
  InvalidateByUrlsRequest
> = z.object({
  urls: z.array(z.string()),
  skipReporting: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    skipReporting: "skip_reporting",
  });
});

export function invalidateByUrlsRequestToJSON(
  invalidateByUrlsRequest: InvalidateByUrlsRequest,
): string {
  return JSON.stringify(
    InvalidateByUrlsRequest$outboundSchema.parse(invalidateByUrlsRequest),
  );
}
export function invalidateByUrlsRequestFromJSON(
  jsonString: string,
): SafeParseResult<InvalidateByUrlsRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InvalidateByUrlsRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InvalidateByUrlsRequest' from JSON`,
  );
}
