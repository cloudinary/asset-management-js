/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Exact model identifier; overrides family/tier when provided. The
 *
 * @remarks
 * `-edit` models accept reference images and are selectable only on
 * `image_to_image`; the others only on `text_to_image`.
 */
export const Id = {
  NanoBanana1: "nano-banana-1",
  NanoBanana2: "nano-banana-2",
  Flux2Klein9b: "flux-2-klein-9b",
  Flux2Pro: "flux-2-pro",
  RecraftV3: "recraft-v3",
  RecraftV4: "recraft-v4",
  GptImage1Mini: "gpt-image-1-mini",
  GptImage2: "gpt-image-2",
  IdeogramV4Base: "ideogram-v4-base",
  IdeogramV4Turbo: "ideogram-v4-turbo",
  NanoBanana1Edit: "nano-banana-1-edit",
  NanoBanana2Edit: "nano-banana-2-edit",
  Flux2Klein9bEdit: "flux-2-klein-9b-edit",
  Flux2ProEdit: "flux-2-pro-edit",
  RecraftV3Edit: "recraft-v3-edit",
  GptImage1MiniEdit: "gpt-image-1-mini-edit",
  GptImage2Edit: "gpt-image-2-edit",
} as const;
/**
 * Exact model identifier; overrides family/tier when provided. The
 *
 * @remarks
 * `-edit` models accept reference images and are selectable only on
 * `image_to_image`; the others only on `text_to_image`.
 */
export type Id = ClosedEnum<typeof Id>;

/**
 * Pin an exact model by ID, bypassing family/tier.
 */
export type ModelById = {
  /**
   * Exact model identifier; overrides family/tier when provided. The
   *
   * @remarks
   * `-edit` models accept reference images and are selectable only on
   * `image_to_image`; the others only on `text_to_image`.
   */
  id: Id;
};

/** @internal */
export const Id$inboundSchema: z.ZodNativeEnum<typeof Id> = z.nativeEnum(Id);
/** @internal */
export const Id$outboundSchema: z.ZodNativeEnum<typeof Id> = Id$inboundSchema;

/** @internal */
export const ModelById$inboundSchema: z.ZodType<
  ModelById,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: Id$inboundSchema,
});
/** @internal */
export type ModelById$Outbound = {
  id: string;
};

/** @internal */
export const ModelById$outboundSchema: z.ZodType<
  ModelById$Outbound,
  z.ZodTypeDef,
  ModelById
> = z.object({
  id: Id$outboundSchema,
});

export function modelByIdToJSON(modelById: ModelById): string {
  return JSON.stringify(ModelById$outboundSchema.parse(modelById));
}
export function modelByIdFromJSON(
  jsonString: string,
): SafeParseResult<ModelById, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ModelById$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ModelById' from JSON`,
  );
}
