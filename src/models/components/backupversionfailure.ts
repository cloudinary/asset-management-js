/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type BackupVersionFailure = {
  /**
   * Hexadecimal version ID; length is a positive multiple of 32 (typically 32 or 64).
   */
  versionId?: string | undefined;
  /**
   * The error message explaining the failure.
   */
  error?: string | undefined;
};

/** @internal */
export const BackupVersionFailure$inboundSchema: z.ZodType<
  BackupVersionFailure,
  z.ZodTypeDef,
  unknown
> = z.object({
  version_id: z.string().optional(),
  error: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "version_id": "versionId",
  });
});
/** @internal */
export type BackupVersionFailure$Outbound = {
  version_id?: string | undefined;
  error?: string | undefined;
};

/** @internal */
export const BackupVersionFailure$outboundSchema: z.ZodType<
  BackupVersionFailure$Outbound,
  z.ZodTypeDef,
  BackupVersionFailure
> = z.object({
  versionId: z.string().optional(),
  error: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    versionId: "version_id",
  });
});

export function backupVersionFailureToJSON(
  backupVersionFailure: BackupVersionFailure,
): string {
  return JSON.stringify(
    BackupVersionFailure$outboundSchema.parse(backupVersionFailure),
  );
}
export function backupVersionFailureFromJSON(
  jsonString: string,
): SafeParseResult<BackupVersionFailure, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BackupVersionFailure$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BackupVersionFailure' from JSON`,
  );
}
