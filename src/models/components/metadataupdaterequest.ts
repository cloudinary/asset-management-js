/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DeliveryTypeAll,
  DeliveryTypeAll$inboundSchema,
  DeliveryTypeAll$Outbound,
  DeliveryTypeAll$outboundSchema,
} from "./deliverytypeall.js";
import {
  StructuredMetadataParam,
  StructuredMetadataParam$inboundSchema,
  StructuredMetadataParam$Outbound,
  StructuredMetadataParam$outboundSchema,
} from "./structuredmetadataparam.js";

/**
 * Structured metadata values plus the assets they apply to.
 */
export type MetadataUpdateRequest = {
  /**
   * The public IDs of the assets to apply the operation to. At most 1000 are processed per request; any beyond the first 1000 are ignored rather than rejected. Public IDs that do not exist, or that the caller lacks update permission for, are skipped rather than failing the request, so the returned `public_ids` may be shorter than the requested list.
   */
  publicIds: Array<string>;
  /**
   * The delivery type of the assets to operate on. Defaults to `upload`.
   */
  type?: DeliveryTypeAll | undefined;
  /**
   * A pipe-separated list or a map of custom metadata fields (by external_id) and the values to assign to each of them. The = " and | characters can be supported as values when escaped with a prepended backslash (\). For a multi-select field, you can set a maximum of 3000 different metadata values on an asset.
   *
   * @remarks
   */
  metadata: StructuredMetadataParam;
  /**
   * Whether to clear a value already stored on the asset that is no longer valid — for example because its datasource entry was deleted — instead of failing the update for that asset. Applies only to fields governed by an active conditional metadata rule; an invalid value sent in this request is always rejected. Default is false.
   */
  clearInvalid?: boolean | undefined;
};

/** @internal */
export const MetadataUpdateRequest$inboundSchema: z.ZodType<
  MetadataUpdateRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  public_ids: z.array(z.string()),
  type: DeliveryTypeAll$inboundSchema.optional(),
  metadata: StructuredMetadataParam$inboundSchema,
  clear_invalid: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    "public_ids": "publicIds",
    "clear_invalid": "clearInvalid",
  });
});
/** @internal */
export type MetadataUpdateRequest$Outbound = {
  public_ids: Array<string>;
  type?: DeliveryTypeAll$Outbound | undefined;
  metadata: StructuredMetadataParam$Outbound;
  clear_invalid?: boolean | undefined;
};

/** @internal */
export const MetadataUpdateRequest$outboundSchema: z.ZodType<
  MetadataUpdateRequest$Outbound,
  z.ZodTypeDef,
  MetadataUpdateRequest
> = z.object({
  publicIds: z.array(z.string()),
  type: DeliveryTypeAll$outboundSchema.optional(),
  metadata: StructuredMetadataParam$outboundSchema,
  clearInvalid: z.boolean().optional(),
}).transform((v) => {
  return remap$(v, {
    publicIds: "public_ids",
    clearInvalid: "clear_invalid",
  });
});

export function metadataUpdateRequestToJSON(
  metadataUpdateRequest: MetadataUpdateRequest,
): string {
  return JSON.stringify(
    MetadataUpdateRequest$outboundSchema.parse(metadataUpdateRequest),
  );
}
export function metadataUpdateRequestFromJSON(
  jsonString: string,
): SafeParseResult<MetadataUpdateRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetadataUpdateRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetadataUpdateRequest' from JSON`,
  );
}
