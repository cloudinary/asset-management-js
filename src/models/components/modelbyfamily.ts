/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Model family:
 *
 * @remarks
 * * `flux`: Photorealistic images (FLUX.2 Klein 9B / FLUX.2 Pro).
 * * `recraft`: Vector and illustration (Recraft V3 / Recraft V4).
 * * `gpt-image`: Campaign and marketing images (GPT Image 1 Mini / GPT Image 2).
 * * `nano-banana`: General purpose generation (Nano Banana 1 / Nano Banana 2).
 * * `ideogram`: Realism, text rendering, and artistic generation (Ideogram V4).
 */
export const Family = {
  Flux: "flux",
  Recraft: "recraft",
  GptImage: "gpt-image",
  NanoBanana: "nano-banana",
  Ideogram: "ideogram",
} as const;
/**
 * Model family:
 *
 * @remarks
 * * `flux`: Photorealistic images (FLUX.2 Klein 9B / FLUX.2 Pro).
 * * `recraft`: Vector and illustration (Recraft V3 / Recraft V4).
 * * `gpt-image`: Campaign and marketing images (GPT Image 1 Mini / GPT Image 2).
 * * `nano-banana`: General purpose generation (Nano Banana 1 / Nano Banana 2).
 * * `ideogram`: Realism, text rendering, and artistic generation (Ideogram V4).
 */
export type Family = ClosedEnum<typeof Family>;

/**
 * Quality tier within the family. Defaults to `standard` when omitted.
 */
export const Tier = {
  Standard: "standard",
  Premium: "premium",
} as const;
/**
 * Quality tier within the family. Defaults to `standard` when omitted.
 */
export type Tier = ClosedEnum<typeof Tier>;

/**
 * Select a model by family and quality tier; a missing `tier` defaults to
 *
 * @remarks
 * `standard`.
 */
export type ModelByFamily = {
  /**
   * Model family:
   *
   * @remarks
   * * `flux`: Photorealistic images (FLUX.2 Klein 9B / FLUX.2 Pro).
   * * `recraft`: Vector and illustration (Recraft V3 / Recraft V4).
   * * `gpt-image`: Campaign and marketing images (GPT Image 1 Mini / GPT Image 2).
   * * `nano-banana`: General purpose generation (Nano Banana 1 / Nano Banana 2).
   * * `ideogram`: Realism, text rendering, and artistic generation (Ideogram V4).
   */
  family: Family;
  /**
   * Quality tier within the family. Defaults to `standard` when omitted.
   */
  tier?: Tier | undefined;
};

/** @internal */
export const Family$inboundSchema: z.ZodNativeEnum<typeof Family> = z
  .nativeEnum(Family);
/** @internal */
export const Family$outboundSchema: z.ZodNativeEnum<typeof Family> =
  Family$inboundSchema;

/** @internal */
export const Tier$inboundSchema: z.ZodNativeEnum<typeof Tier> = z.nativeEnum(
  Tier,
);
/** @internal */
export const Tier$outboundSchema: z.ZodNativeEnum<typeof Tier> =
  Tier$inboundSchema;

/** @internal */
export const ModelByFamily$inboundSchema: z.ZodType<
  ModelByFamily,
  z.ZodTypeDef,
  unknown
> = z.object({
  family: Family$inboundSchema,
  tier: Tier$inboundSchema.optional(),
});
/** @internal */
export type ModelByFamily$Outbound = {
  family: string;
  tier?: string | undefined;
};

/** @internal */
export const ModelByFamily$outboundSchema: z.ZodType<
  ModelByFamily$Outbound,
  z.ZodTypeDef,
  ModelByFamily
> = z.object({
  family: Family$outboundSchema,
  tier: Tier$outboundSchema.optional(),
});

export function modelByFamilyToJSON(modelByFamily: ModelByFamily): string {
  return JSON.stringify(ModelByFamily$outboundSchema.parse(modelByFamily));
}
export function modelByFamilyFromJSON(
  jsonString: string,
): SafeParseResult<ModelByFamily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ModelByFamily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ModelByFamily' from JSON`,
  );
}
