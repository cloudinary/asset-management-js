/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DeclarativeImageSize,
  DeclarativeImageSize$inboundSchema,
  DeclarativeImageSize$Outbound,
  DeclarativeImageSize$outboundSchema,
} from "./declarativeimagesize.js";
import {
  DimensionsImageSize,
  DimensionsImageSize$inboundSchema,
  DimensionsImageSize$Outbound,
  DimensionsImageSize$outboundSchema,
} from "./dimensionsimagesize.js";

/**
 * Desired output size, given in **one** of two mutually exclusive forms:
 *
 * @remarks
 *
 *   * `DimensionsImageSize`: `width` and `height` in pixels, for precise
 *     control.
 *   * `DeclarativeImageSize`: `aspect_ratio` (and an optional `resolution`
 *     tier), resolved server-side to the closest size the chosen model
 *     supports. This is the portable form: most providers natively accept
 *     an aspect ratio plus a resolution tier rather than raw pixels.
 *
 * Omit `image_size` entirely to use the model's default size.
 */
export type ImageSize = DimensionsImageSize | DeclarativeImageSize;

/** @internal */
export const ImageSize$inboundSchema: z.ZodType<
  ImageSize,
  z.ZodTypeDef,
  unknown
> = z.union([
  DimensionsImageSize$inboundSchema,
  DeclarativeImageSize$inboundSchema,
]);
/** @internal */
export type ImageSize$Outbound =
  | DimensionsImageSize$Outbound
  | DeclarativeImageSize$Outbound;

/** @internal */
export const ImageSize$outboundSchema: z.ZodType<
  ImageSize$Outbound,
  z.ZodTypeDef,
  ImageSize
> = z.union([
  DimensionsImageSize$outboundSchema,
  DeclarativeImageSize$outboundSchema,
]);

export function imageSizeToJSON(imageSize: ImageSize): string {
  return JSON.stringify(ImageSize$outboundSchema.parse(imageSize));
}
export function imageSizeFromJSON(
  jsonString: string,
): SafeParseResult<ImageSize, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageSize$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageSize' from JSON`,
  );
}
