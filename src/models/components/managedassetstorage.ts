/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Cloudinary resource type of the stored asset.
 */
export const ManagedAssetStorageResourceType = {
  Image: "image",
  Video: "video",
  Raw: "raw",
} as const;
/**
 * Cloudinary resource type of the stored asset.
 */
export type ManagedAssetStorageResourceType = ClosedEnum<
  typeof ManagedAssetStorageResourceType
>;

/**
 * A generated asset stored as a permanent managed asset. Reference it in
 *
 * @remarks
 * other Cloudinary APIs by `asset_id`, or by the
 * `resource_type` / `type` / `public_id` triple.
 */
export type ManagedAssetStorage = {
  /**
   * Discriminator identifying this as managed-asset storage.
   */
  storageType: "managed_asset";
  /**
   * The URL to fetch the generated asset.
   */
  secureUrl: string;
  /**
   * Cloudinary asset ID — a stable, globally-unique reference to the asset.
   */
  assetId?: string | undefined;
  /**
   * Public ID of the stored asset.
   */
  publicId: string;
  /**
   * Cloudinary resource type of the stored asset.
   */
  resourceType: ManagedAssetStorageResourceType;
  /**
   * Cloudinary delivery type of the stored asset (e.g. `upload`, `private`, `authenticated`).
   */
  type: string;
  /**
   * Version of the stored asset.
   */
  version: number;
};

/** @internal */
export const ManagedAssetStorageResourceType$inboundSchema: z.ZodNativeEnum<
  typeof ManagedAssetStorageResourceType
> = z.nativeEnum(ManagedAssetStorageResourceType);
/** @internal */
export const ManagedAssetStorageResourceType$outboundSchema: z.ZodNativeEnum<
  typeof ManagedAssetStorageResourceType
> = ManagedAssetStorageResourceType$inboundSchema;

/** @internal */
export const ManagedAssetStorage$inboundSchema: z.ZodType<
  ManagedAssetStorage,
  z.ZodTypeDef,
  unknown
> = z.object({
  storage_type: z.literal("managed_asset"),
  secure_url: z.string(),
  asset_id: z.string().optional(),
  public_id: z.string(),
  resource_type: ManagedAssetStorageResourceType$inboundSchema,
  type: z.string(),
  version: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "storage_type": "storageType",
    "secure_url": "secureUrl",
    "asset_id": "assetId",
    "public_id": "publicId",
    "resource_type": "resourceType",
  });
});
/** @internal */
export type ManagedAssetStorage$Outbound = {
  storage_type: "managed_asset";
  secure_url: string;
  asset_id?: string | undefined;
  public_id: string;
  resource_type: string;
  type: string;
  version: number;
};

/** @internal */
export const ManagedAssetStorage$outboundSchema: z.ZodType<
  ManagedAssetStorage$Outbound,
  z.ZodTypeDef,
  ManagedAssetStorage
> = z.object({
  storageType: z.literal("managed_asset"),
  secureUrl: z.string(),
  assetId: z.string().optional(),
  publicId: z.string(),
  resourceType: ManagedAssetStorageResourceType$outboundSchema,
  type: z.string(),
  version: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    storageType: "storage_type",
    secureUrl: "secure_url",
    assetId: "asset_id",
    publicId: "public_id",
    resourceType: "resource_type",
  });
});

export function managedAssetStorageToJSON(
  managedAssetStorage: ManagedAssetStorage,
): string {
  return JSON.stringify(
    ManagedAssetStorage$outboundSchema.parse(managedAssetStorage),
  );
}
export function managedAssetStorageFromJSON(
  jsonString: string,
): SafeParseResult<ManagedAssetStorage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ManagedAssetStorage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ManagedAssetStorage' from JSON`,
  );
}
