/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A reference to an external image by HTTPS URL.
 */
export type UrlReference = {
  /**
   * Discriminator identifying this as an external-URL reference.
   */
  sourceType: "url";
  /**
   * HTTPS URL of the reference image.
   */
  url: string;
};

/** @internal */
export const UrlReference$inboundSchema: z.ZodType<
  UrlReference,
  z.ZodTypeDef,
  unknown
> = z.object({
  source_type: z.literal("url"),
  url: z.string(),
}).transform((v) => {
  return remap$(v, {
    "source_type": "sourceType",
  });
});
/** @internal */
export type UrlReference$Outbound = {
  source_type: "url";
  url: string;
};

/** @internal */
export const UrlReference$outboundSchema: z.ZodType<
  UrlReference$Outbound,
  z.ZodTypeDef,
  UrlReference
> = z.object({
  sourceType: z.literal("url"),
  url: z.string(),
}).transform((v) => {
  return remap$(v, {
    sourceType: "source_type",
  });
});

export function urlReferenceToJSON(urlReference: UrlReference): string {
  return JSON.stringify(UrlReference$outboundSchema.parse(urlReference));
}
export function urlReferenceFromJSON(
  jsonString: string,
): SafeParseResult<UrlReference, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UrlReference$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UrlReference' from JSON`,
  );
}
