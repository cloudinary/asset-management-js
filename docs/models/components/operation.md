# Operation

The operation to perform on the principal’s role assignments. `add` grants the specified roles; `remove` revokes them.

## Example Usage

```typescript
import { Operation } from "@cloudinary/asset-management/models/components";

let value: Operation = "add";
```

## Values

```typescript
"add" | "remove"
```